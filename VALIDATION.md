# 验证记录

2026-09-17，当前 Apple Silicon Mac，Swift 5.10 / Command Line Tools。

## 已执行并通过

- Release 构建、`.app` 打包与本地 ad-hoc 签名。
- **25 项核心测试**：SRT CRLF/BOM/往返解析、非法时间/重叠、中文单行、字幕半开区间、新增字幕边界、同轨调整范围、显示开关、重复 ID、工程版本/越界/持久化、译文重排/漏项/重复/未翻译、横竖屏坐标和边缘限制。
- **9 项服务测试**：缺失依赖报错、子进程取消、超时、翻译失败保留法语、缓存续传、导出取消、保留已有目标、清除未完成临时视频、禁止覆盖源视频。
- **真实生成流程**：系统 Thomas 法语语音合成约 9 秒测试视频 → ffmpeg 音频提取 → MLX Whisper → 本机 Codex → 3 条法语与 3 条中文，编号和时间戳对应。
- **实际窗口测试**：本地视频选择、新增中文字幕、文本实时更新、添加法语字幕、字号 `58,5` 输入、撤销、重做、退出重开后恢复文字和样式。右侧属性面板默认从顶部显示；通过保存面板写出 `UI-Test.frzh`，通过导出按钮生成 `UI-export.mp4`，状态显示导出完成。
- **实际视频导出**：640×360 / 24 fps 有音轨视频、360×640 / 30 fps 无音轨视频、带 90° 显示矩阵的视频。输出 H.264；有音轨时为 AAC，时长均为 3 秒。旋转视频输出为 360×640。
- 抽帧检查了字幕切换前后、双语布局、中文字体和背景。旋转方向与源视频自动旋转后的画面一致。非字幕区域与源画面的平均通道误差小于 5/255（有损重编码）。

## 可复现命令

```sh
./scripts/check.sh
./scripts/build-app.sh
./scripts/smoke-media.sh
```

原生视频编码测试在正常 macOS 环境运行；Codex 执行沙箱屏蔽部分媒体服务，沙箱中 AVAssetWriter 音频设置可能无法通过验证。真实生成测试使用本机账号；取消/失败回归使用本地假工具，不发起网络调用。

## 尚未覆盖

长视频性能与内存压力、所有第三方编码格式、HDR 保真、Intel Mac 和其他系统版本。本版本定位当前 Mac 自用，不提供安装依赖自动化、公证或商店分发。

## 中文字幕批次校验修复（2026-09-17）

实际失败原因：法语存在独立的 `?` 字幕，Codex 返回 `？`，旧逻辑因该条没有汉字而拒绝整批译文。

修复后允许内容等价的纯标点和数字字幕（忽略全半角与空白差异），继续拒绝空译文、未翻译的法语、数字被改写以及缺失/重复 ID。错误信息包含问题字幕的时间。

新增 6 项回归测试，31 项核心测试全部通过；用用户原失败批次额外校验通过。已构建并重启修复版，点击继续生成后复用缓存，窗口显示「法中字幕已生成 · 15 组」，15 条中文字幕均已载入。

## 语言筛选与播放跟随（2026-09-17）

法语、中文改为两个独立的多选开关，列表和预览区开关同步。筛选同时用于列表、时间轴和视频字幕。新增字幕使用当前选中字幕的语言，单语言模式使用该语言。

播放和定位时，列表自动选中所有当前有效的可见字幕并滚动到对应行；双语可同时高亮两行。程序更新选中状态时屏蔽表格回调，避免触发 seek 导致播放循环。开始编辑文字或数值时暂停播放。

33 项核心测试通过，新增四种语言选择组合与播放边界/间隙测试；Release 构建通过。当前用户仍在操作旧窗口，本次没有完成重启后的 GUI 验证，需重新打开应用启用新版。

## 鼠标缩放与自动语言样式（2026-09-17）

预览选中框提供四角字号缩放手柄，主体拖动调整位置；每个拖动事件同步属性面板，鼠标释放时仅注册一条撤销记录，Esc 可取消未完成拖动。预览拖动和属性面板的样式修改统一应用到同语言轨道，清除该语言单条样式覆盖；文本与时间编辑仍只影响当前字幕。

37 项核心测试通过，新增同语言样式传播、另一语言隔离、内容/时间/ID 保持、新字幕继承、四角缩放和上下限测试。实际桌面应用中，将中文字幕从 44 拖动到约 72.5，右侧字号更新，自动保存中 15 条中文字幕均使用新轨道样式。通过一次撤销恢复拖动前字号和单条覆盖。已更新桌面与 dist 两个应用副本并启动桌面新版。

## 左右宽度手柄（2026-09-17）

左右中点新增横向拖动手柄，调整文本框宽度与换行，固定另一侧边缘并限制在视频内；四角继续调整字号。右侧宽度百分比实时更新，10–100% 可输入，留空恢复自动布局。同语言样式传播与一次拖动一次撤销保持一致。

42 项核心测试通过，新增左右边界约束、旧工程解码、宽度持久化与字号不变测试。Release 编译通过，已更新并启动桌面应用。在用户工程中实际拖动右侧手柄，宽度更新为 88.9%，字号保持 60.2；一次撤销恢复自动宽度和原位置。预览和导出共用含宽度字段的排版逻辑。

## 属性面板宽度（2026-09-17）

属性面板从 280 加宽到 340，使用滚动视图实际可用宽度布局，并预留右侧间距。属性行和文字编辑器随内容宽度调整，避免固定宽度输入框被滚动条裁切。

Release 构建及桌面应用签名校验通过，已更新并打开桌面版本。在现有工程中选中字幕，确认字号、宽度、颜色、描边和位置控件右边完整可见。

## 面板显示开关（2026-09-17）

顶部新增素材、字幕属性、时间轴三个独立开关，隐藏面板后预览使用释放的空间，UserDefaults 保存各开关状态。切换前提交正在编辑的输入框内容。

Release 构建与桌面副本签名校验通过。实际窗口逐个收起三个面板，确认仅预览时布局扩大；再逐个展开，确认素材列表、属性控件和时间轴恢复，顶部开关始终可操作。已更新桌面应用。

## 独立说明文字轨道（2026-09-17）

新增多条文字轨道、目标轨道选择与添加文字入口。文字片段复用时间编辑、拖动/边缘调整、预览选择及共用导出绘制；样式按独立轨道传播，法中显示开关与重新生成保留文字轨道。工程用可选轨道字段兼容旧数据，轨道 ID/同轨重叠/时长边界均校验。时间轴支持纵向滚动。

47 项核心测试通过，包含旧工程解码、多轨同时显示、语言开关独立、样式隔离、持久化和无效轨道检测。Release 构建通过并更新桌面版。GUI 实测新增两秒文字片段，预览出现说明文字；右侧结束时间改为 4 秒，列表同步 0–4 秒；两次撤销分别恢复时长和移除测试轨道。修复定位回调把新文字选中状态切回中文字幕的问题。本次未另行导出样片验证。

## 时间轴选中手柄（2026-09-17）

选中字幕/文字片段使用白色边框和左右白色抓手，手柄优先命中、鼠标显示横向调整光标，中间区域使用移动光标。选中时间轴片段时暂停播放，定位保持当前有效片段的选中状态；仅在时段确实变化时注册编辑。

Release 构建通过并更新桌面副本。实际窗口截图确认选中中文字幕片段显示完整边框和左右手柄。用户正在操作应用，本次未额外改动其片段进行拖动测试。

## Delete 删除片段（2026-09-17）

主编辑窗口支持 Delete / Fn+Delete 删除当前选中字幕或文字片段，复用工程撤销。输入框与文本编辑器保留原生删字行为，弹窗、任务运行期间不处理片段删除；忽略按住键产生的重复删除。时间轴点击会正确取得键盘焦点。

Release 构建通过并更新桌面应用。GUI 实测选中首条法语字幕后按 Delete，片段从列表移除；⌘Z 恢复原字幕，原工程内容已恢复。

## 背景自适应文字宽度（2026-09-17）

将背景矩形宽度与编辑文本框宽度分离，按相同换行约束测量文字宽度后居中绘制背景，保留左右内边距。多行按最长行宽度包围，编辑框仍用于调整换行范围。预览和导出共用此绘制逻辑。

Release 构建通过并更新桌面应用；实际工程截图确认「医生您好」黄色背景紧贴四字宽度，不再铺满整行。

## 点击空白取消选中（2026-09-17）

预览非字幕区域、时间轴空白、素材列表空白及编辑器背景支持取消选中，清除预览边框、时间轴手柄和列表高亮，属性面板恢复未选中状态。保留字幕命中和属性编辑行为；同一组当前字幕不会被播放回调立即重新选中，进入下一组字幕后恢复播放跟随。

Release 构建通过并更新桌面版。实际界面确认取消后字幕仍显示、预览与时间轴边框消失、列表无选中、属性控件禁用。

## 文本编辑框左侧裁切（2026-09-17）

文字编辑器宽度与实际裁剪区同步，禁止水平滚动偏移，保留 12 点左右内边距并统一行片段边距。Release 构建通过并更新桌面版；实测选中「医生您好」，首字完整显示且左侧有明确留白。

## 删除文字轨道（2026-09-17）

时间轴顶部新增「删除轨道」，按下拉框稳定 ID 删除文字轨道及关联片段，复用完整工程快照撤销；无轨道或任务运行时禁用。Release 构建通过并更新桌面应用。实际界面确认文字轨道删除后下拉框清空，删除按钮禁用，原法中字幕仍保留。

## 白色选中框样式（2026-09-17）

预览选中框改为白色细线、四角圆点和左右圆角手柄，添加轻微阴影以便在浅色画面识别。原有移动、四角字号缩放和左右调宽命中范围保留。此次为外观调整，未添加参考图中的旋转功能。

Release 构建通过并更新桌面应用。实际选中「通过新闻学法语」确认白色边框与圆形角点可见。

## 小字幕角点缩放命中修复（2026-09-17）

角点与侧边手柄改为就近选择，避免小字号时两类命中区重叠导致角点拖动被侧边调宽抢占；光标区域也按高度缩小区分。Release 构建通过并更新桌面版。GUI 实测「通过新闻学法语」左下角拖动，字号从 30.8 更新为 64.0，右侧属性同步；一次撤销恢复 30.8。

## 文字可读性增强（2026-09-17）

增加可读性开关及兼容旧工程的可选样式字段。默认增强无实色背景的浅色文字，使用对比描边和阴影，最后单独绘制原色字面避免小字号被描边吞没。49 项核心测试通过，含默认策略和开关持久化。Release 构建通过，桌面应用实际截图确认法语白色字面与深色轮廓同时可见，黑字黄底保持原样。本次未额外导出样片。

## 上下区域高度调节（2026-09-17）

上下区域之间增加 10 点高可拖动分隔线，中央绘制提示条，提供上下调整光标和辅助功能 splitter 标识。拖动实时调整全部上方面板与时间轴高度，限制最小高度，结束保存偏好，双击恢复默认；隐藏时间轴时同时隐藏。

Release 构建通过并更新桌面版。实际窗口观察到分隔线位置变化、预览和时间轴随之重新布局，重启后调整高度保留。UI 自动化部分拖动收到用户操作冲突提示，未继续打断用户反复验证。

## 左右面板分隔线

右侧属性面板最小宽度由 340 点降低 10% 至 306 点；拖动、窗口布局及已保存宽度恢复使用同一限制，默认和双击重置宽度仍为 340 点。Release 重新构建通过并更新桌面版。

素材与预览、预览与属性之间新增可左右拖动的分隔线，带中央竖向提示条、左右调整光标及辅助功能标签。限制面板最小宽度，为预览保留空间；拖动结束保存宽度，双击恢复默认，隐藏面板时隐藏对应分隔线。素材列表列宽及文字随面板伸缩。

Release 构建通过，已更新桌面应用。实际界面分别向右拖动左分隔线、向左拖动右分隔线，确认两侧面板变宽，素材文字显示范围扩大，预览随剩余空间缩放。此轮未新增自动化测试或重复视频导出验证。

## 右侧面板默认宽度 300

默认与双击重置宽度改为 300 点，最小宽度同步调整为 300 点，避免默认值被最小值抬高。已有手动调整宽度继续保留，可双击分隔线恢复新的默认值。Release 构建通过并更新桌面应用。

## 素材与字幕分类（2026-09-18）

左下改为「素材 / 字幕」单选分类。素材页只列当前视频，字幕页按法语、中文及文字轨道列名称与条数，不显示逐句内容。类别切换独立于预览语言显示；字幕选择和播放跟随映射至所属轨道，具体字幕继续通过时间轴或预览编辑。素材页添加按钮导入视频，字幕页添加按钮添加对应字幕或文字。

Release 构建成功，49 项核心检查通过。桌面版实际确认素材页只有 test.mp4，字幕页列出法语字幕 14 条、中文字幕 14 条、文字 1 共 1 条；点击中文字幕后右侧显示「医生您好」及相应属性，预览语言开关仍同时开启。首次并行运行检查遇到构建数据库锁，改为顺序构建后检查通过。

## 播放时隐藏预览选中框（2026-09-18）

预览选择显示与编辑选择分离：播放期间预览 selected 为 nil，开始播放立即刷新，播放进度跟随和工程刷新也遵循此规则。时间轴及属性仍跟随当前字幕，暂停后可以继续选中编辑。
Release 构建通过并更新桌面应用。实际播放确认双语字幕正常显示、预览没有边框或手柄、时间轴当前片段仍高亮；验证后已暂停。

## 多视频剪辑、特效与 HDR 检测（2026-09-18）

增加兼容旧工程的可选视频片段模型，包含源时间范围、稳定 ID、叠化和调色/淡入淡出参数。顺序拼接、前后排序、分割、删除、裁剪均可撤销；字幕随源片段重新映射，叠化中点切换同轨字幕。素材添加支持多选；视频时间轴显示独立片段，边缘可裁剪，双击打开参数面板。新增「视频剪辑」菜单。

预览和导出共用 AVMutableComposition、自定义浮点合成器、音频混合及色彩管理。不同尺寸等比居中。生成字幕使用剪辑后音频。导出检测源传递曲线，SDR/mixed 提供 SDR，全 HLG/PQ 提供对应 HEVC Main10。禁止覆盖任何源片段。HDR 为重新编码，不保留 Dolby Vision 动态元数据或 HDR10 母版/峰值静态元数据；未用真实 Dolby Vision 素材验证。

验收结果：
- Release 构建通过；核心检查 56/56，包括旧工程兼容、裁剪/排序字幕映射、分割 ID、叠化边界、非法参数、持久化和删除。
- `smoke-media.sh` 横屏、竖屏、旋转、无音轨导出均通过，音轨 AAC、视频 H.264、尺寸和时长正确；9 项服务检查通过（取消、超时、翻译失败恢复、目标文件保护等）。受限执行环境首次媒体进程异常退出，正常系统媒体服务环境复跑通过。
- `check-editing.sh` 最终通过：2.5 秒与 3 秒片段叠化 0.5 秒，输出 5 秒；转写输入混音时长匹配；实际导出和 ffprobe 检查 HLG/PQ 为 HEVC Main10、yuv420p10le、BT.2020、正确传递函数。HDR 转 SDR 输出 BT.709。混合素材不提供 HDR。
- HLG/PQ 灰阶高亮测试：10-bit 亮度码平均误差分别 0.735 / 0.709，最大误差 2，输入最大码值 936、输出 935，高亮区保留 103 个不同码值。此为合成素材数值验证，不代表所有真实 HDR 素材或屏幕的观感验证。
- 检视叠化中点导出帧，确认两视频叠加和双语字幕；AppKit 实际检查裁剪/叠化/调色输入面板及源素材检测、输出格式列表。实际 GUI 未逐项测试所有排序与拖动组合；核心时间映射及真实合成导出已有上述覆盖。

最终媒体测试产物：`/tmp/video-editeur-editing.y9czmm`；横竖屏测试：`/tmp/video-editeur-smoke.TbipCg`。已更新桌面应用。

## 时间轴分割快捷按钮（2026-09-18）

增加三个带括号/虚线图标的按钮：播放头分割、删除片段左侧、删除片段右侧。附提示和辅助功能标签，空工程、任务执行中或播放头不在目标片段内部时禁用。分割复用原有流程；左右裁剪调整源时间范围、淡入淡出及叠化上限，通过既有字幕映射和撤销流程提交。

Release 构建通过；核心检查 59/59，包括左右裁剪后的字幕映射、后续片段位移以及边界拒绝。已更新桌面应用，并确认三个按钮出现在时间轴工具栏、播放头位于起点时禁用。GUI 定位播放头时连续收到用户操作冲突提示，因此没有继续自动执行分割/撤销，也未将该项记录为已验证。

## 视频轨道连续画面预览（2026-09-18）

将单张重复缩略图改为每片段多时间点采样，统一旧单视频和多片段加载入口。按裁剪后的源起止范围采样，后台分批更新；取消旧任务并检查任务身份，防止旧片段结果覆盖新工程。绘制时依据时间位置选图，等比居中裁切填充，文件名增加半透明暗底。每片段最多 160 张，避免长视频无界采样。

Release 构建通过并更新桌面应用；实际截图确认视频轨道已连续显示当前素材画面，播放预览正常。此轮未改变导出链路，未重复导出测试。

## 框选区域去字（2026-09-18）

预览新增「区域去字」按钮和框选状态，Delete 打开覆盖设置，支持自动逐帧取底边背景、手动颜色、坐标及时间范围；菜单管理区域。使用可选片段字段保存源时间和归一化坐标，兼容旧工程，分割后继承区域。浮点视频合成器在新增字幕前处理覆盖，预览与导出共用，支持撤销。已修复框选被全局取消选择提前终止的交互，并在 mouseUp 补算末次指针位置。

实际素材发现音频/容器总时长略长于画面轨道。合成器现在按有效交集插入画面，用首/尾样本补齐边界时间，避免原有严格时长检查误拒绝；不改变音频或字幕时间。

- Release 构建通过，核心检查 61/61，包含去字参数/时间校验、序列化和分割后的继承。
- `--smoke-erase` 验证自动蓝色背景覆盖、手动红色、生效区间及实际导出。输出中选区像素由 [255,255,255] 变成 [1,2,255]，新增中文字幕保持可见。
- 额外使用 2 秒画面 + 2.2 秒音频素材验证边界补齐及导出，成功。
- 实际桌面版验证点击工具、拖动框选、Delete、设置面板和应用；当前新闻视频蓝底条文字成功消失，背景保留。⌘Z 恢复原文字，已撤销该验证修改。
- 不承诺纹理修复、运动跟踪或图案复原；自动取色依赖选区底边为干净背景。此轮未重复 HDR 灰阶测试。

## 空格播放快捷键（2026-09-18）

增加主编辑窗口 keyDown 处理，空格切换播放 / 暂停，消费长按重复事件；文本编辑、组合键、弹窗和其他窗口交还原事件。菜单移除原本默认带 Command 的空格快捷键，标明实际空格操作，避免干扰文字输入。Release 构建通过，已更新桌面应用；实测空格启动、22.015 秒暂停、再按继续至 22.366 秒，最后按空格暂停成功。本轮未修改媒体管线，未重复导出测试。

## 字幕对齐（2026-09-18）

新增字幕框内左 / 中 / 右对齐，旧工程缺省为居中；背景自动贴合文字并随对齐移动。预览与导出共用排版器，属性通过原有同轨样式、撤销及持久化流程更新。Release 构建通过，核心检查 63/63，覆盖旧工程兼容、序列化和轨道隔离。`--check-subtitle-alignment` 三项排版检查通过，验证多行文本段落对齐及背景边缘/中心位置。桌面应用已更新，界面确认对齐控件默认「中」；未重复视频导出测试。

## 默认字幕字体（2026-09-18）

法语默认 AvenirNextCondensed-Regular / 60，中文 ArialMT / 54，仅更新语言轨道初始化样式，已有工程及独立文字轨道保持原样。Release 构建通过；本机 NSFont 验证两个 PostScript 字体名分别解析为 Avenir Next Condensed 与 Arial。已更新并启动桌面应用。

## 新建工程（2026-09-18）

新增顶部按钮及 ⌘N；重置工程、播放器、缩略图任务、字幕属性、选择、时间轴缩放、重试任务关联和撤销记录，覆盖空白恢复文件并清除旧工程关联。新建前比较已保存工程，未保存时提供保存 / 放弃 / 取消，保存成功才继续；生成或导出中禁止新建。Release 构建通过，桌面实测按钮清空已保存工程，时间归零、素材和文字轨道清空，再次启动仍为空白。未实际执行放弃未保存修改的路径。

## 中英文界面（2026-09-18）

添加中文 / English 字典及带参数文本渲染，默认中文，语言偏好保存在 editor.interfaceLanguage，下次启动生效。设置和应用菜单均可选择；菜单入口不依赖外部工具配置。覆盖主界面、菜单、字幕属性、剪辑/去字面板、生成状态及核心校验错误，保留字幕数据和翻译提示词；英文使用更紧凑的按钮文字与较宽时间轴标签。

Release 构建通过，65/65 核心检查通过；包含译文占位符一致性、未知键回退、中文文件名与占位符形式内容不被替换。两项旧测试同步修复字号 44 的过时假设。实际桌面验证首次中文、切换英文后重启持久化、英文主界面/属性/设置；最后再构建调整英文轨道名宽度。未重复媒体导出测试。

## 去字工具高亮（2026-09-18）

区域去字改为切换按钮，进入框选后同步 on 状态、青色底和深色文字；再次点击、Esc、开始播放或完成去字通过退出入口恢复默认外观。Esc 同时清理选中片段关联。Release 构建通过，桌面应用更新后确认按钮初始 off、框选时 on；用户继续操作中，未打断当前框选。

## 框选起点取色（2026-09-18）

鼠标按下时捕获归一化起点和当前帧时间，后台读取带旋转/视频合成的图像，以 sRGB 取该像素；排除字幕选择框等 UI。任务标识避免取消或重新框选后的旧结果覆盖；取色失败时提示重新框选。新区域默认保存取到的不透明颜色，自动底边取色默认关闭。Release 构建通过；Core Image 坐标上下方向、边缘取样检查在允许图形服务的环境通过。实际视频从青绿背景框选，面板颜色为 RGB 0.376471/0.647059/0.568627，自动匹配为关闭；取消测试并 Esc 退出，未修改工程内容。

## 背景音乐与原声静音（2026-09-18）

增加可选 backgroundMusic / muteVideoAudio 工程字段，兼容旧工程；音乐使用稳定 ID、源范围、时间轴起点和音量。加入独立绿色时间轴轨道、拖动移动/边缘裁剪、编辑、删除及撤销。AVComposition 为每段音乐插入音轨并按视频结尾截断；静音时排除视频原声音轨。预览与导出共用混音；转写主动排除背景音乐并恢复视频原声。原视频和音乐文件均禁止作为导出覆盖目标。

- Release 构建及 67/67 核心检查通过，包含工程往返、旧默认和非法音乐参数。
- --check-music 实际导出混音与静音两个 MP4；2 秒视频加 3 秒 MP3，音乐从 0.5 秒开始，输出长度保持 2 秒。
- 频率检查：混音中 440 Hz 原声约 4104、880 Hz 音乐约 1943；静音后分别约 0.01 和 1943。音乐开始前静音输出为零，验证起点与静音结果。测试产物在 /tmp/video-editeur-music-check。
- 桌面版实测中文空格文件名 MP3 导入，绿色音乐轨道、编辑面板默认 30% 音量正确，⌘Z 成功撤销测试导入，恢复原工程。
- 未重复 HDR 画面测试；音乐为固定音量、不自动循环，不包含波形显示。

## 统一素材与音乐剪切（2026-09-18）

移除三个音乐专用按钮，左侧添加素材和菜单支持视频/音频混合选择，素材列表同步显示音乐。共用分割、删左、删右及编辑/删除入口按当前音乐选择路由；定位时间尺保留选择。音乐裁剪保持绝对时间，不改变视频/字幕，分割右片段使用新 ID 并继承音量与源范围。Release 构建通过，70/70 检查通过（新增源时间映射、两侧裁剪、边界及视频隔离检查）。桌面实际选择 Come Alive.mp3，播放头 10 秒处点击共用按钮分成 10.00 / 34.58 秒，两段之外四段视频保持不变；⌘Z 恢复原音乐。保留原来的静音状态。

## 保留完整音乐时长（2026-09-18）

修复导入音乐时按视频剩余长度裁短源范围的问题。时间轴长度和音乐片段显示使用完整音频范围，可横向滚动查看；右侧裁剪边缘可恢复到源音频结尾。预览和导出仍以视频时长为限。已有工程的裁剪范围保持不变，重新导入可获取完整范围。Release 构建及 71/71 检查通过，新增 41 秒视频搭配 120 秒音乐的范围、持久化及视频时长隔离检查；桌面应用已更新。

## 应用图标（2026-09-20）

打包脚本从 Assets/Brand/video-editeur-logo-v1.png 生成标准 16–1024 像素 iconset，经 iconutil 生成 AppIcon.icns，并设置 CFBundleIconFile。受限环境的 iconutil 返回 Invalid Iconset；在正常 macOS 权限环境重新打包成功。Release 构建、Info.plist 图标引用及 codesign --verify --deep --strict 检查通过，桌面应用已更新。未强制重启正在运行的应用；运行中 Dock 图标可能需退出重开刷新。
